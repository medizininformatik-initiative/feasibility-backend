spring:
  datasource:
    url: "jdbc:tc:postgresql:13.1-alpine:///codex_ui_test"
security:
  config:
    use-keycloak: false

app:
  queryResultExpiryMinutes: 1
  privacy:
    quota:
      create:
        amount: 3
        intervalMinutes: 1
      read:
        any:
          pollingIntervalSeconds: 0
