spring:
  datasource:
    url: "jdbc:tc:postgresql:13.1-alpine:///codex_ui_test"
security:
  config:
    use-keycloak: false

app:
  queryResultCacheExpiryMinutes: 1
  security:
    nqueries:
      amount: 3
      perminutes: 1
